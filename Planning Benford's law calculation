

class BenCalc{
    public static void main (String[] args) {
      double sample[] = {9109, 1239, 4119, 2339}; 
    }
    
    //Finds the first number and returns the number of time this numer was shiwn 
    //Add docstrings
    public static void firstNumber (String[] args) {
    }
    
    //Calculates the percentage of the number of times shown the first number X is shown
    //Returns the percentage which will be used in the graph 
    //Add docstrings 
    public static void percentage (String[] args) {
      
      //Benfords law states that the number x should appear approximately x/9%  
      //We can get this percentage by the number of times the number is shown which is then divided y the total number 
      //of numbers
      //If it falls out of the approx range then it is an indication of fraud
      
      //Maybe use if statements and validate the percentages depending on the digit(x). 
      
      
      //Use print statements to check if we're correct
      
    }
    
    
    //Generates a bar chart
    public static void graph (String[] args) {
    } 
}
